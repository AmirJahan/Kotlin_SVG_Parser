package com.oddinstitute.svgparser

import android.graphics.PointF

val svgSplitChar = '|'

// all of these exist in Moush
/*
const val MyTag = "MyTag"

var canvasHdRatio = 1f

var pivotCanvas = PointF()
var offsetCanvas: PointF = PointF()
var scaleCanvas: Float = 1f

val scaleSeekbarSize = 10f
val scaleSeekbarFactor = 10
val scaleSeekbarMax = (scaleSeekbarSize * 2 * scaleSeekbarFactor).toInt() // 200
val rotateSeekbarMax = 720
val strokeSeekbarMax = 50

const val MaxPositive: Int = Int.MAX_VALUE
const val MinNegative: Int = Int.MIN_VALUE

val colorRange = 30


// TODO this height needs to be determined better
val clipHeight: Int = 44
val clipTopMargin: Int = 8


val typesThatShouldReturn1 = listOf(Artwork::class, Polygon::class)
fun processObject(obj: Any): Boolean {
    if (typesThatShouldReturn1.any { it.isInstance(obj) }) {
        return true
    }
    return false
}

 */
